pre-commit:
  jobs:
    - name: patch-ui-ts-nocheck
      run: pnpm --dir apps/web ui:patch-ts-nocheck
      stage_fixed: true

    - name: format
      run: pnpm format
      stage_fixed: true

    - name: lint
      run: pnpm lint

    - name: check-types
      run: pnpm check-types
